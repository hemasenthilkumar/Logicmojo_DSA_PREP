"""
1. Check if a given number is prime number

Approach : Iterate till square root of the number and check if its divisible O(sqrt(n))

2. Get all prime numbers within given number

Approach 1: Naive Approach

For each number between the range, check if the number is prime number O(n.sqrt(n))

Approach 2: Sieve of Eratosthenes

For each number, we strike off the further multiples untill the range
"""